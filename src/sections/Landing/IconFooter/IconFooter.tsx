import React, { FC } from "react";
import Icon from "../../../components/Icon/Icon";

const IconFooter = () => {
  return (
    <div>
      <Icon />
      <Icon />
      <Icon />
    </div>
  );
};

export default IconFooter;
